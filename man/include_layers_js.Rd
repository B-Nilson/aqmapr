% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/include_layers_js.R
\name{include_layers_js}
\alias{include_layers_js}
\title{Include js in a Leaflet map header with references to layers}
\usage{
include_layers_js(
  map,
  base_maps = c(`Light Theme` = "OpenStreetMap"),
  point_layers = list(),
  polygon_layers = list(),
  wms_layers = list()
)
}
\arguments{
\item{base_maps}{(Optional).
A character vector named basemaps from \link[leaflet:providers]{leaflet::providers} to add to the map.
Names will be used in the control menu for the basemaps if provided.
Default is a nice light and dark open data theme.}

\item{point_layers, polygon_layers, wms_layers}{(Optional).
A list of 1 or more \code{PointLayer}/\code{PolygonLayer}/\code{WMSLayer} objects (created with \code{\link[=PointLayer]{PointLayer()}}/\code{\link[=PolygonLayer]{PolygonLayer()}}/\code{\link[=WMSLayer]{WMSLayer()}}) to be added to the map.
Default is an empty list (no points/polygons/WMS layers added).}
}
\value{
A leaflet map with the script file(s) included in the page header
}
\description{
Include js in a Leaflet map header with references to layers
}
\examples{
library(leaflet)
library(aqmapr)

leaflet() |>
  add_base_maps(base_maps = "OpenStreetMap") |>
  include_layers_js(
    base_maps = c("Light Theme" = "OpenStreetMap"),
    point_layers = list(),
    polygon_layers = list(),
    wms_layers = list()
  )
}
