% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_map_state.R
\name{track_map_state}
\alias{track_map_state}
\title{Track active map layers in Leaflet and view in the URL}
\usage{
track_map_state(
  map,
  js_dir = system.file("js", package = "aqmapr"),
  js_endpoint = "/js",
  as_reference = FALSE
)
}
\arguments{
\item{map}{A leaflet map object}

\item{js_dir}{The directory where custom js files are stored.
This should not need to be changed.
Default is the "js" directory in the \code{aqmapr} package.}

\item{js_endpoint}{The endpoint where custom js files can be accessed by the client.
Typically this is either a ambiorix/plumber endpoint or a static directory
(relative to the html file being served).}

\item{as_reference}{(Optional).
If TRUE, the paths will be included in the src attribute of the script tag.
If FALSE, the paths will be read in and embeded directly.
Default is FALSE.}
}
\value{
A leaflet map object
}
\description{
Track map state (layers, center, zoom) and update URL accordingly.
On page load, check URL for state and apply to map.
}
\examples{
library(leaflet)
library(aqmapr)

leaflet() |>
  add_base_maps(base_maps = "OpenStreetMap") |>
  track_map_state()
}
