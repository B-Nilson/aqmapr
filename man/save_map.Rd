% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_map.R
\name{save_map}
\alias{save_map}
\title{Save a leaflet map to a file}
\usage{
save_map(
  map,
  save_to = "./index.html",
  self_contained = FALSE,
  library_dir = NULL,
  encoding = "UTF-8",
  page_title = "My Map",
  ...
)
}
\arguments{
\item{map}{A leaflet map}

\item{save_to}{The file path to save the map to.
File extension must be one of ".html", ".png", ".pdf", or ".jpeg".
Default is \code{"./index.html"}.}

\item{self_contained}{Should the map be written to a standalone file (\code{TRUE}) or a file linking to dependencies written to a folder named after \code{library_dir} (\code{FALSE}).
Ignored if \code{save_to} is not a ".html" file (as those are always self-contained).
Default is \code{FALSE}.}

\item{library_dir}{The directory to write dependencies to when \code{self_contained} is \code{FALSE}.
Default is \code{NULL}, in which case dependencies are written to a folder named after \code{save_to} + "_files".}

\item{encoding}{The encoding to use when writing the file.
Default is \code{"UTF-8"}.}

\item{page_title}{The title of the page.
Default is \code{"My Map"}.}

\item{...}{Additional arguments to pass to \link[mapview:mapshot]{mapview::mapshot} (which calls \link[webshot:webshot]{webshot::webshot} for non-HTML files, and \link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget} for HTML).}
}
\description{
Largely a wrapper around \link[mapview:mapshot]{mapview::mapshot}, allowing for saving a leaflet map to a file.
}
